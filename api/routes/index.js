const projectsRouter = require('./projects');
const tasksRouter = require('./tasks');
const usersRouter = require('./users');

module.exports = {
    tasksRouter,
    projectsRouter,
    usersRouter,
};
